# Testing

We use [Amen](https://github.com/dashkite/amen) as the primary testing framework for Masonry. The test suite is designed to ensure that pipelines can be properly assembled and that all module exports function correctly.

## Running Tests

To run the test suite, execute the test script directly from the project root:

```bash
./scripts/test
```
